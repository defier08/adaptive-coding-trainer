---
name: adaptive-coding-trainer
description: 当需要进行交互式、自适应编程练习时使用本 skill——例如用户说"出道题练练""讲讲闭包并出个练习""结合我的项目代码出个题""帮我按遗忘曲线复习薄弱点""这道题我卡住了,针对它出几道变体"。本 skill 将知能行考研数学的自适应刷题思路迁移到编程学习:两层粒度(知识点原子 + 模块链路)、三文件练习(示例/模板/测试)、实现级主流方案教学、SM-2 掌握度跟踪、变体题防背题、三场景统一闭环(项目驱动 / 卡点驱动 / 系统化)。与 codequiz、kp-explain 的关键区别在于:以业界主流规范方案为权威,用户已有项目代码仅作"引出话题的线索 / 对照纠偏",绝不作为标准答案。
---

# Adaptive Coding Trainer

## Overview

一个以知能行（自适应刷题）为蓝本、迁移到编程学习的交互式训练器。核心目标：让用户用更少但更准的题量，把编程知识点（不限于前端，涵盖 JS 基础 / 算法 / Node 后端 / 通用概念）真正练会，并靠间隔复习对抗遗忘。

本 skill 解决普通题库的两个致命缺陷：
1. **"背题"而非"真会"** —— 通过同一知识点的多道变体题 + SM-2 间隔复习解决。
2. **"散点"而非"成网"** —— 通过两层粒度（知识点原子 + 模块链路）与依赖图解决。

## 与现有 skill 的定位差异（务必先读）

| 维度 | codequiz / kp-explain（已有） | 本 skill（adaptive-coding-trainer） |
|------|------------------------------|--------------------------------------|
| 教学基准 | **锚定用户项目代码**（"项目用 RSA+AES 就讲 RSA+AES，别扯通用方案"） | **以业界主流规范方案为权威**；用户项目仅作线索 / 对照，可能本身就是不规范的反例 |
| 颗粒度 | 单一知识点维度 | 两层：知识点原子层 + 模块链路层（教"怎么把点穿起来"） |
| 防背题 | 有去重 | 变体题库 + SM-2，检验"会不会"而非"记没记住" |
| 适用 | 通用问答 / 出题 | 专门的编程自适应训练闭环 |

> 冲突铁律提醒：现有 skill 要求"必挂项目、锚定真实代码"，本 skill **反其道而行**——通用学习以调研的主流方案为基准。仅当用户明确要求"结合我的具体项目代码"时，才以该项目为入口，但仍须对照主流方案指出其规范与否。

## 首次使用配置

本 Skill **开箱即用**：默认无需任何配置，学习数据自动存储在用户级 `~/.codebuddy/adaptive-coding/`（`progress.json` / `library/` / `modules/`）。

配置项说明：
- `ADAPTIVE_CODING_DIR`（可选）：学习数据根目录。留空使用默认 `~/.codebuddy/adaptive-coding`；可通过环境变量覆盖（引擎运行时读取，优先级最高），派生路径 `progress.json` / `library/` / `modules/` 随之变化。
- 首次使用可运行 `node scripts/trainer.mjs init` 在默认目录初始化数据；`scan` 会自动扫描 `library/`。

配置文件位置：`config.example.json`（与 SKILL.md 同目录，记录配置项说明）；本 skill 的实际配置机制为环境变量 `ADAPTIVE_CODING_DIR`。
修改配置：随时告诉我"重新配置 adaptive-coding-trainer"即可重新引导。

## Core Workflow（出题 → 写 → 验证 → 推进 闭环）

对每一次练习，严格按以下顺序执行：

1. **选题**
   - 用户指定主题（如"闭包""Promise"）→ 直接定位或新建知识点。
   - 用户贴项目代码 → 从代码挖出涉及的知识点，**题目落盘到 `library/`（`~/.codebuddy/adaptive-coding/library`，不进项目目录）**，再 `enqueue --kp <知识点名> --source project` 入队（场景二）。
   - 用户踩坑 / 报错 → 沉淀成针对性题目（同样落 `library/`），再 `enqueue --kp <知识点名> --source stuck` 入队（场景三）。
   - 否则调用 `node scripts/trainer.mjs pick`，引擎按优先级（选题队列：卡点 > 项目驱动 > SM-2 到期 > 新题）自动选题（pick 会自动 scan，无需手动 init/scan）。
2. **落盘题目文件**
   - 知识点层：生成 `example.js`（教，必有，最小化）+ `template.js`（练，必有，含 `// TODO`）+ `test.js`（验，按需）。规范见 `references/file-convention.md`。
   - 模块层：生成 `01-方案选型.md` / `02-推荐方案实现.md` / `03-调用链.md` / `04-知识点清单.md` / `05-综合练习`。规范见 `references/module-layer.md`。
3. **用户填写**：用户仅在 `template.js` 的 `// TODO` 处写实现（或完成模块层综合练习）。
4. **运行验证**
   - 有 `test.js`：`node <dir>/test.js`，断言逐条判定。
   - 无 `test.js`：概念 / 时序题，直接 `node <dir>/template.js` 观察输出，或由用户说明预期。
5. **客观判定质量分 q**（依据 `references/sm2-algorithm.md` 的 q 表，客观为主）。
6. **推进 SM-2**：`node scripts/trainer.mjs review-next --kp <知识点名> --q <0-5> --variant <第1步返回的变体标识>`。务必带上第 1 步 `pick` 返回的 `variant`，否则防背题的变体记录会退化为根题。
7. **循环**：回到第 1 步，引擎会优先推到期复习与薄弱点。

## 两层粒度（必须分清）

- **知识点层（原子）**：可单独检验的最小单元（如"数组去重""闭包""事件循环"）。用三文件（example / template / test）教与练。同一知识点备 2~3 道变体防背题。
- **模块层（链路）**：一组知识点按"方案 / 调用链"串起来（如"登录模块""权限模块"）。轻量一等学习单元，教的是"装配图"——怎么把一堆杂知识点穿成能跑的模块。可嵌套（大模块套子模块）。模块不重复教知识点，链上每点回落到知识点层练。

## 教学基准原则（实现级）

- 调研业界主流规范方案，给出：① 选型对比（几种主流 + 原理 + 适用场景）② 完整可运行规范实现 ③ 最佳实践 ④ 常见坑。
- 用户项目代码若出现偏差，借机对照主流方案纠偏（"你的写法 vs 主流写法，差在哪、为什么该改"）。
- 示例文件坚持**最小化、以教会知识点为先**，不做无关复杂演示。

## 自适应组织（数据组织）

- **物理层**：题目平铺编号 `library/001-xxx/`（统一用户级，不进项目目录），稳定不变。
- **逻辑层**：每个 `template.js` 顶部写头部元数据：
  ```js
  /**
   * @kp          数组去重
   * @tags        数组, 算法, Set
   * @difficulty  易
   * @prereq      数组遍历, 相等性判断
   */
  ```
- **视图层**：由 `scripts/trainer.mjs view`（按 tags 分组 / difficulty 排序）与 `graph`（prereq 依赖图 + 薄弱前置）动态生成。换视图不改文件。依赖关系随错题归因反哺、渐进生长，`pick` 的新题阶段会按依赖图拓扑推进、暴露缺失前置（`blocked_by`）。

## Resources

本 skill 自带确定性引擎与规范文档，执行时按需加载：

- `scripts/trainer.mjs` — Node 零依赖引擎：扫描题目与元数据、按优先级选题（选题队列 + 依赖图拓扑 + 难度）、SM-2 推进、到期复习列表、掌握度查询、依赖图（`graph`）与视图（`view`）。所有"必须跨会话确定性发生"的副作用（进度、间隔、选题队列）都落在这里，不要让 AI 口头记录。
- `references/file-convention.md` — 知识点层三文件规范（example / template / test）+ 头部元数据格式 + 变体目录结构。
- `references/module-layer.md` — 模块层五文件规范（方案选型 / 实现 / 调用链 / 清单 / 综合）+ 实现级主流基准要求。
- `references/sm2-algorithm.md` — SM-2 三要素、质量分 q 客观判定表、变体防背题规则。
- `references/scenarios.md` — 三场景闭环（项目驱动 / 卡点驱动 / 系统化）与选题优先级。

> 调用引擎前先确认 Node 可用（`node --version`）。学习数据默认存于用户级 `~/.codebuddy/adaptive-coding/`：`progress.json`（全局掌握度，按 @kp 名索引、跨项目共享）、`library/`（**全部题目题库**，含通用主题与从项目 / 卡点挖的题，统一存放、不进项目目录）、`modules/`（通用模块实现级讲解）。**该根目录为可配置项**，可用环境变量 `ADAPTIVE_CODING_DIR` 覆盖，派生路径 `progress.json` / `library/` / `modules/` 随之变化。`scan` 默认扫 `library`，`--dir` 可选附加任意题集合目录（不要指向项目源码目录），进度始终汇总进同一份全局进度，由 trainer 原子写入。
